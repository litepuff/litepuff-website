import { BaseBusinessService } from './BaseBusinessService.js'; import { SHEET_NAMES } from '../../config/sheets.js'; import { validateCoupon } from '../../validation/domainValidation.js'; import { validationError } from '../../utils/AppError.js';
export class CouponService extends BaseBusinessService { constructor(dependencies = {}) { super({ sheet: SHEET_NAMES.COUPONS, primaryKey: 'CouponID', validator: validateCoupon, ...dependencies }); } async validate(code, subtotal) { const row = await this.sheets.readOne(this.sheet, (item) => String(item.Code).toUpperCase() === String(code).toUpperCase()); if (!row || String(row.Status).toLowerCase() !== 'active') throw validationError('Invalid coupon code.'); if (row.Expiry && new Date(row.Expiry) < new Date()) throw validationError('This coupon has expired.'); if (Number(row.MinOrder || 0) > Number(subtotal)) throw validationError('This order does not meet the minimum coupon value.'); return row; } }
export const couponService = new CouponService();
